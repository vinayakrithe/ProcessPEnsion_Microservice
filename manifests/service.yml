apiVersion: v1
kind: Service
metadata:
    name: vinayakritheprocesspensionmicroservice
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: vinayakritheprocesspensionmicroservice